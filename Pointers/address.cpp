#include <iostream>
using namespace std;

int main(){
    // int a = 10;
    // int *ptr1 = &a;

    // cout << "Address of 'a' through 'a' : " << &a << endl;

    // cout << "Address of 'a' through 'ptr1' : " << ptr1 << endl;

    // cout << "Address of 'ptr1' through 'ptr1' : " << &ptr1 << endl;

    // cout << "Value of 'a' through 'ptr1' : " << *ptr1 << endl;

    // float price = 102.25f;
    // float* ptr = &price;

    // cout << endl;

    // int** ptr2 = &ptr1; // Pointer to pointer

    // cout << "Address of 'a' through 'ptr2' : " << *ptr2 << endl;

    // cout << "Address of 'ptr1' through 'ptr2' : " << ptr2 << endl;

    // cout << "Address of 'ptr2' through 'ptr2' : " << &ptr2 << endl;

    // cout << "Value of 'a' through 'ptr2' : " << **ptr2 << endl;

    int *ptr = NULL;
    
    cout << "Value of 'ptr' : " << ptr << endl;

    return 0;
}